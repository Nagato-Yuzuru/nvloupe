# Changelog

Notable changes, newest first. Format follows
[Keep a Changelog](https://keepachangelog.com); versions follow
[SemVer](https://semver.org) once there's something to version.

## [Unreleased]

### Added

- Project scaffold: crate skeleton (lib + thin CLI), CI on Linux/macOS/Windows
  (tokenless Codecov via OIDC), prek hooks, CodeRabbit config, issue and PR
  templates, and the docs.

Nothing scans yet. The `scan` and `diff` commands parse their arguments and
report that they aren't implemented.
